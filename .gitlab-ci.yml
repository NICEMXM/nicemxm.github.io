#before_script:
#  - apt-get update -qq && apt-get install -y -qq pandoc
#image: node:4.2.2
#pages:
#  cache:
#    paths:
#    - node_modules/
#  script:
#  - npm install hexo-cli -g
#  - npm install
#  - npm uninstall hexo-renderer-marked --save
#  - npm install hexo-renderer-pandoc --save
#  - npm install hexo-generator-sitemap --save
#  - npm install hexo-generator-baidu-sitemap --save
#  - npm install hexo-util --save
#  - hexo deploy
#  artifacts:
#    paths:
#    - public
#  only:
#  - master

image: node:20.13.1 # use nodejs v16 LTS
cache:
  paths:
    - node_modules/

before_script:
#  - npm install hexo-cli -g
#  - npm install hexo-renderer-markdown-it-plus --save  # 新增
#  - npm install hexo-math --save # 新增
#  - npm install hexo-generator-feed --save # 新增
#  - npm install hexo-next-giscus --save # 新增
  - npm install

pages:
  script:
    - npm run build
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
